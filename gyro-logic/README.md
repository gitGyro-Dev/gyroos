# Gyro Logic

Formal logic layer of the GyroOS project.

This directory is intended to contain the observer-relative logic model,
formal definitions, inference rules, operator system, type system, semantics,
and computational model of Gyro Logic.
